from distutils.core import setup
import os

setup(
    name='boostksp',
    version='${PACKAGE_VERSION}',
    author='Laurent Lejeune',
    author_email='laurent.lejeune@artorg.unibe.ch',
    description='boost ksp',
    packages=['boostksp'],
    package_dir={'boostksp': '${CMAKE_CURRENT_BINARY_DIR}'},
    package_data = {'boostksp': ['libksp.so']}
)
